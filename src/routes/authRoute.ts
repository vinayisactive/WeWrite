import { Hono } from "hono";

const singupRoute = new Hono(); 

singupRoute.post("/", async({req, res}) => {
    try {
        
    } catch (error) {
        
    }
}); 


const singinRoute = new Hono(); 

singinRoute.post(("/"), async({req, res}) => {
    try {
        
    } catch (error) {
        
    }
})


export  {singupRoute, singinRoute}; 